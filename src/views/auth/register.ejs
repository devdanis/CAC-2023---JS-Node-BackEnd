<!DOCTYPE html>
<html lang="es">

<head>
	<meta charset="UTF-8" />
	<meta http-equiv="X-UA-Compatible" content="IE=edge" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	<link rel="stylesheet" href="/css/login_shop-style.css">
	<title>Register</title>
</head>

<body>
	
	<%- include('../partials/header.ejs') %>
	
	<main>

		<section class="main__login">
		
			<h1>CREAR TU CUENTA</h1>
			
			<% if( typeof error != 'undefined'){ %>
				<div class="error">
			
					<% error.forEach(err =>{ %>
				
						<p><%= err.msg %></p>
					
					<% }) %>
					
				</div>
			<% } %>
			<form class="form__login" method="post" action="/auth/register">
				<div class="form__div">
					<label class="form__label" for="user_name">Nombre:</label>
					<input id="user_name" type="text" name="user_name" value="<%= typeof values != 'undefined' ? values.user_name : '' %>" placeholder="Juan" />
				</div>
				<div class="form__div">
					<label class="form__label" for="user_last_name">Apellido:</label>
					<input id="user_last_name" type="text" name="user_last_name" value="<%= typeof values != 'undefined' ? values.user_last_name : '' %>" placeholder="Ojeda" />
				</div>
				<div class="form__div">
					<label class="form__label" for="email">Email:</label>
					<input id="user_email" type="email" name="user_email" value="<%= typeof values != 'undefined' ? values.user_email : '' %>" placeholder="jonhdoe@correo.com" />
				</div>
				<div class="form__div">
					<label class="form__label" for="user_password">Contraseña:</label>
					<input id="user_password" type="password"  name="user_password"  />
				</div>
				<div class="form__div">
					<label class="form__label" for="repassword">Repita Contraseña:</label>
					<input id="repassword" type="password" name="repassword"  />
				</div>

				<div class="form__div login">
					<button class="form__button" type="submit">Registrarse</button>
					<input id="term-of-use" name="term-of-use" type="checkbox" />
					<label class="form__label-checkbox" for="term-of-use">Acepto termino y condiciones </label>
				</div>
			</form>
			<a href="#">¿Olvido su contraseña?</a>
		</section>

	</main>
	<%- include('../partials/footer.ejs') %>
</body>

</html>